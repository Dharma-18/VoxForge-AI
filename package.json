{
  "name": "voxforge-ai",
  "private": true,
  "version": "0.0.0",
  "description": "VoxForge AI – A Futuristic Conversational AI IDE with 3D Interactive Voice Robot",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
